<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ra.boot.mapper.PicsMapper">

    <resultMap id="BaseResultMap" type="PicsEntity">
            <id property="picsId" column="pics_id" jdbcType="INTEGER"/>
            <result property="pic" column="pic" jdbcType="VARCHAR"/>
    </resultMap>


    <select id="getPics" resultType="PicsEntity">
        select * from pics
    </select>

    <insert id="insertPics" useGeneratedKeys="true" keyProperty="picsId">
        insert into pics(`pics_id`,`pic`)
                    values(#{picsId},#{pic})
    </insert>

</mapper>
